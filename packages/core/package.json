{
    "name": "@circuitjs/core",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "test": "vitest run",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@circuitjs/shared": "*"
    },
    "devDependencies": {
        "vite-tsconfig-paths": "^6.1.1"
    }
}
