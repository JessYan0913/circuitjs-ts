{
    "name": "@circuitjs/electron",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist/main.js",
    "scripts": {
        "build": "tsc",
        "dev": "npm -w packages/ui run build && tsc && electron .",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@circuitjs/mcp-server": "*"
    },
    "devDependencies": {
        "electron": "^43.0.0"
    }
}
